function add(a, b) {
  return a + b;
}

function subtract(a, b) {
  return a - b;
}

function multiply(a, b) {
  return a * b;
}

function divide(a, b) {
  return a / b;
}

let operandA = "";
let operator = "";
let operandB = "";

function operate(operator, operandA, operandB) {
  switch (operator) {
    case "+":
      //...
      break;
    case "-":
      //...
      break;
    case "*":
      //...
      break;
    case "/":
      //...
      break;
  }
}
